export const config = {
   apiUrl: 'http://etama.id:8004',
};

/*export function config(Param) {
  return fetch(Url + `/param/listbygroup/JENISKEBUTUHAN`, {method: 'GET'});
}

export const API = {
  getBanner: (type: Banner, categoryId: number) =>
    `/banner/bannermobile?target=${type}&categoryid=${categoryId}`,
  getAdvancedSearch: '/product/advancesearch',
  getCategories: (groupCode: GroupCodeType, parentId: number) =>
    `/category/mobile/${groupCode}/${parentId}`,
  login: '/customers/login',
  sendOTP: (phoneNumber: string) => `/customers/phoneotp?mode=send&phoneno=${phoneNumber}`,
  resetPassword: (phoneNumber: string) => `/customers/byphone/${phoneNumber}`,
  getProductById: (productId: string) => `/product/byid/${productId}`,
  getSKUListById: (productId: string) => `/sku/listbyproduct/${productId}`
};*/

